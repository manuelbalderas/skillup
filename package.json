{
  "name": "skillup",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node ./server/server.js",
    "client": "cd client && npm start",
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
